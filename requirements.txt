# Local pipeline dependencies (document generation, verification, QC, MCQ generation).
#
# NOTE: the `false_facts` library and its heavier deps (anthropic, transformers, ...) come from the
# upstream clone, not from here. After cloning safety-research/false-facts and its submodule, install it:
#     cd $FALSE_FACTS_REPO && git submodule update --init && uv pip install -e .
# The Kaggle finetune+eval notebook (src/eval/) installs its own GPU deps (peft/trl/bitsandbytes) in-cell.

openai>=1.58
python-dotenv>=1.0
scikit-learn>=1.3      # near-duplicate detection (TF-IDF + cosine) in the QC checks
numpy>=1.24
scipy>=1.10
fire>=0.5             # CLI entrypoints used by some upstream-style scripts
tqdm>=4.66
